name: Enterprise Test Automation CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  api-tests:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 25

      - name: Cache Maven Dependencies
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-

      - name: Run API Test Suite
        run: mvn test -DsuiteFile=src/test/resources/testng-api.xml

  ui-tests:
    runs-on: ubuntu-latest

    env:
      # UTEM reporter reads this env var to know where to POST results.
      # Set UTEM_SERVER_URL in GitHub → Settings → Secrets and variables → Actions.
      # If the secret is absent the reporter falls back to localhost:8080 and
      # logs "Send failed" warnings, but tests still run and results are correct.
      UTEM_SERVER_URL: ${{ secrets.UTEM_SERVER_URL }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 25

      - name: Install Chrome
        uses: browser-actions/setup-chrome@v1

      - name: Run UI Test Suite (Headless)
        run: |
          mvn test \
          -DsuiteFile=src/test/resources/testng-ui.xml \
          -Dheadless=true

      - name: Upload Screenshots on Failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: ui-screenshots
          path: target/screenshots